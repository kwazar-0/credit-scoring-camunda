# Camunda Deployment

Deployment assets for Camunda runtime and workflow packages.

- `helm/` or `manifests/` should contain release-ready deployment configuration.
- `bpm/` and `dmn/` should contain versioned workflow artifacts.
