# Workload Identity: set annotation to Terraform output backend_gcp_service_account_email.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend-sa
  namespace: hbg
  annotations:
    iam.gke.io/gcp-service-account: backend@PROJECT_ID.iam.gserviceaccount.com
